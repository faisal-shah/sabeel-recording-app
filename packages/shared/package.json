{
  "name": "@sabeel/shared",
  "version": "0.0.0",
  "private": true,
  "sideEffects": false,
  "main": "lib/index.js",
  "types": "lib/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^3.2.0"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  }
}
